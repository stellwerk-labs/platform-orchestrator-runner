apiVersion: v2
name: platform-orchestrator-kubernetes-agent-runner
description: A Helm chart for the Platform Orchestrator Kubernetes Agent Runner
type: application
version: 0.3.0
appVersion: "v3.0.0"

dependencies:
  - name: nats
    alias: protected-nats
    version: "2.14.0"
    repository: "https://nats-io.github.io/k8s/helm/charts/"
    condition: protected-nats.enabled
